# Allowlist for scripts/check_no_secrets_in_urls.sh
#
# Format:
#   <path-substring>|<line-substring>
#
# Use this only for cases where query-string auth is mandated by a third-party
# protocol and cannot be moved to headers/body without breaking compatibility.

# (empty)
